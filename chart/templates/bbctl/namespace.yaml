{{- include "bigbang.normalizePackageAliases" . -}}
{{- if and .Values.bbctl.enabled .Values.monitoring.enabled }}
{{- include "bigbang.namespace" (dict "root" . "name" "bbctl" "appName" "bbctl" "package" .Values.addons.gitlab) }}
{{- end }}
