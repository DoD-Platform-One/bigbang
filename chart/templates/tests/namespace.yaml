{{- if dig "bbtests" "enabled" false (merge .Values dict) }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ template "kyverno-policies.name" . }}-bbtest
  labels:
    {{- include "kyverno-policies.test-labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
{{- end }}