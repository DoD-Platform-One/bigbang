# yaml-language-server: $schema=https://raw.githubusercontent.com/helm-unittest/helm-unittest/main/schema/helm-testsuite.json
suite: Schema - Umbrella
tests:
  - it: should pass validation with valid umbrella values (empty)
    asserts:
      - notFailedTemplate: {}

  - it: should fail validation with invalid extra key in umbrella
    set:
      foo: bar
    asserts:
      - failedTemplate: {}

  - it: should reject the removed automatic passthrough compatibility setting
    set:
      disableAutomaticPassthroughValues: false
    asserts:
      - failedTemplate: {}
