# yaml-language-server: $schema=https://raw.githubusercontent.com/helm-unittest/helm-unittest/main/schema/helm-testsuite.json
suite: Package - SonarQube - Istio Annotation Routing
templates:
  - templates/sonarqube/values.yaml
tests:
  - it: should render the istio roll annotation on the upstream pod template
    postRenderer:
      cmd: "sh"
      args:
        - "-c"
        - "yq eval '.stringData.defaults' - | yq eval '.'"
    set:
      addons.sonarqube.enabled: true
      istio.ambient.enabled: true
    asserts:
      - equal:
          path: upstream.annotations["bigbang.dev/istioDataplane"]
          value: ambient
      - isNull:
          path: annotations
