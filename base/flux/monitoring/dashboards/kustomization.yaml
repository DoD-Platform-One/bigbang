apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  grafana_dashboard: "1"
  release: monitoring
configMapGenerator:
- name: flux-dashboards
  files:
  - cluster.json
  - control-plane.json
