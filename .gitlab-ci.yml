include:
  - project: 'big-bang/pipeline-templates/pipeline-templates'
    ref: 'mapping-things'
    file: '/pipelines/bigbang-package.yaml' 
variables:
  PIPELINE_REPO_BRANCH: 'mapping-things'
  DEBUG_ENABLED: "true"
  BB_REPO: repo1.dso.mil/snekcode/bigbang
  BB_PROJECT_ID: 12985
  CI_PROJECT_NAME: "mattermost"
  BB_AUTO_MR_TOKEN: ${SNEKCODE_FORK_TOKEN}
  FOO: "BAR"
  CI_DEFAULT_BRANCH: "mnagel-mm-main"
