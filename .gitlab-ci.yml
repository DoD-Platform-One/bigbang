include:
  - project: 'platform-one/big-bang/pipeline-templates/pipeline-templates'
    ref: helm-update
    file: '/pipelines/helm.yaml' 
variables:
  PIPELINE_REPO_BRANCH: helm-update
  DEBUG_ENABLED: "true"
